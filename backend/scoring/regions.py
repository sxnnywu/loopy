"""Vertices -> top region/network per second -> region_timeline. Owner: B."""
def region_timeline(vertex_timeseries) -> list:
    raise NotImplementedError  # TODO(B): -> [{t, top_network, top_region, activation}, ...]
